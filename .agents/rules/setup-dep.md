---
trigger: model_decision
description: When deciding on orchestration and setups for the project
---

1. Exclusively use `uv` and avoid anything to do with `pip`.

2. For building and serving the project, use `uv run mkdocs build` and `uv run mkdocs serve`.